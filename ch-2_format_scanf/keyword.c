#include <stdio.h>

int main(){

    // will generate error if we take reserve keyword as variable or function name 

    // there are total 32 keyword which we can't take as variable name or function name 

    int int = 10;
    
    return 0;
}