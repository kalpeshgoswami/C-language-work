#include <stdio.h>

int main()
{

    int drivingLicence = 1;

    if (drivingLicence == 1)
    {
        printf("you can drive vehicle");
    }
    else
    {

        printf("you can't drive vehicle");
    }

    return 0;
}